using Messenger.CoreData;

namespace Messenger.GeneralKingdomData.MonoBehaviour
{
    public class Kingdom : UnityEngine.MonoBehaviour
    {
        private KingdomData _kingdomData;
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
